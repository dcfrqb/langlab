/* ============================================================
   CONTENT — сборка уроков медицинского курса из трёх частей.

   Части идут по главам учебников, темы внутри части — в порядке
   книги: так «листать подряд» означает идти по First Aid, а не по
   случайному набору. Номера (n) сквозные — это позиция в рейке
   курса, и считать их руками не надо.

   Каждая тема — выжимка: мысль → таблица-сравнение или дерево
   решений → термины → проверка. У шагов стоит ссылка на страницу
   учебника, её проверяет tools/medicine/check.mjs.
   ============================================================ */
import { CARDIO } from './lessons/cardio.js';
import { ENDO } from './lessons/endo.js';
import { EMERGENCY } from './lessons/emergency.js';

export const GROUPS = [
  'Часть 1 · Кардиология',
  'Часть 2 · Эндокринология',
  'Часть 3 · Неотложное и травма',
];

export const LESSONS = [...CARDIO, ...ENDO, ...EMERGENCY]
  .map((lesson, i) => ({ ...lesson, n: i + 1 }));

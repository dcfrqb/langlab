/* ============================================================
   CONTENT — сборка курса IELTS из четырёх частей.

   Порядок частей — это порядок подготовки, а не порядок экзамена:
   сначала понять правила игры (иначе тренируешься не в то),
   потом чтение (там приёмы дают прибавку быстрее всего),
   потом письмо (там прибавка самая медленная — начинать надо раньше),
   и справочная часть, к которой возвращаются постоянно.

   Номера (n) сквозные — это позиция в рейке курса, руками их
   считать не надо.
   ============================================================ */
import { FORMAT } from './lessons/format.js';
import { READING } from './lessons/reading.js';
import { WRITING } from './lessons/writing.js';
import { LEXIS } from './lessons/lexis.js';

export const GROUPS = [
  'Часть 1 · Как устроен экзамен',
  'Часть 2 · Чтение по приёмам',
  'Часть 3 · Письмо по кирпичикам',
  'Часть 4 · Лексика, шаблоны и правила',
];

export const LESSONS = [...FORMAT, ...READING, ...WRITING, ...LEXIS]
  .map((lesson, i) => ({ ...lesson, n: i + 1 }));
